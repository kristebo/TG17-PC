<div class="container">
<div class="jumbotron text-center">
<h1>
Vigenere crypto
</h1>
<p>
kristebo
</p>
<p>
60
</p>
</div>
<div>
  <h2> Oppgave </h2>
  <pre>
  alphabet = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz .,?-_;:+1234567890"'

  def vigenere_encode(msg, key):
      """Function that encodes a string with Vigenere cipher. The encrypted
         string is returned.
      """
      secret = ''
      key_length = len(key)
      alphabet_length = len(alphabet)

      for i, char in enumerate(msg):
          msgInt = alphabet.find(char)
          encInt = alphabet.find(key[i % key_length])

          if msgInt == -1 or encInt == -1:
              return ''

          encoded = (msgInt + encInt) % alphabet_length
          secret += alphabet[encoded]

      return secret
    <pre>
    Jeg trenger en decodefunksjon for vigenere. Kan du lage et program som finner nøkkelen til denne krypterte meldingen:

    <pre>
    CIe2CORFN,8CFEX1LXV;FG,Q?me:6IBG0 BEH
    </pre>
</div>
<div>
<h2> input </h2>
 den krypterte meldingen
<h2> output </h2>
 decryptert melding på første linje, nøkkelen på andre.
</div>
<div>
  <div class="container">
  <h2> eksempler </h2>
  <div class="table-responsive">
      <table class="table">
      <thead>
        <tr>
          <th><b>INPUT</b></th>
          <th><b>OUTPUT</b></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>
          <pre>
              CIe2CORFN,8CFEX1LXV;FG,Q?me:6IBG0 BEH
          </pre>
          </td>
          <td>
          [dekrypter melding]
          [nøkkel]
          </td>
        </tr>
      </tbody>
    </table>
  </div>
  </div>
</div>
